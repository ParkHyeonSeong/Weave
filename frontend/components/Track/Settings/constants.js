// Backwards-compatible re-export. New code should import from '@/library/entityAppearance'.
import { DEFAULT_COLORS } from '@/library/entityAppearance';
export { COLOR_PRESETS, HEX_RE, DEFAULT_COLORS } from '@/library/entityAppearance';
export const DEFAULT_TRACK_COLOR = DEFAULT_COLORS.track;
