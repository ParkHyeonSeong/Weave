# Weave host root (no trailing /api). Tools append /api/... themselves.
WEAVE_BASE_URL=https://weave.example.com

# Weave Personal Access Token (Profile → Personal Access Tokens in the Weave UI).
# Sent as an Authorization: Bearer header. Treat it like a password.
WEAVE_API_TOKEN=wv_your_token_here
